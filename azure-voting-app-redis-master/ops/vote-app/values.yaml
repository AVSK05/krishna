replicaCount: 1

image:
  repository: depnonprodregistry.azurecr.io/eframe-services/dev/graphql-apollo-starter
  tag: __version__
  pullPolicy: Always

service:
  type: LoadBalancer
  port: 8080

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
nodeSelector: {}

tolerations: []

affinity: {}
